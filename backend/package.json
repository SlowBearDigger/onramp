{
  "name": "offramp-backend",
  "version": "0.1.0",
  "description": "Transak webhook receiver + orders API for the offramp frontend.",
  "private": true,
  "type": "module",
  "main": "app.js",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node app.js",
    "dev": "node --watch app.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^5.2.1",
    "express-rate-limit": "^7.5.1",
    "helmet": "^8.1.0",
    "jose": "^5.10.0"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
